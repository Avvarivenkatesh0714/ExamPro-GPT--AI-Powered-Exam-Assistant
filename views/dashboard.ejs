<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Dashboard - ExamPro AI</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Fugaz+One&family=Inter&family=Poppins&family=Ubuntu&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: "Inter", sans-serif;
      background-color: white;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 800px;
      margin: 40px auto;
      padding: 30px;
      background-color: #ffffff;
      border-radius: 12px;
      margin-top: 35px;
    }

    h2 {
      color: #343a40;
      text-align: center;
    }

    a {
      color: #007bff;
      text-decoration: none;
      display: inline-block;
      margin-bottom: 20px;
    }

    label {
      font-weight: bold;
      color: #495057;
    }

    textarea {
      width: 100%;
      height: 100px;
      margin: 10px 0 20px;
      padding: 10px;
      border-radius: 6px;
      border: 1px solid #ced4da;
      resize: vertical;
      font-size: 16px;
    }

    button {
      background-color: black;
      color: white;
      padding: 10px 18px;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      cursor: pointer;
    }

    button:hover {
      background-color: white;
      color: black;
      border: 2px solid black;
    }

    .answer-box {
      background-color: #e9f7ef;
      border-left: 6px solid black;
      padding: 20px;
      border-radius: 10px;
      margin-top: 15px;
      color: #155724;
      font-size: 16px;
      white-space: pre-line;
    }

    hr {
      margin: 40px 0;
      border: 0;
      border-top: 1px solid #dee2e6;
    }

    .flashes {
      list-style: none;
      padding-left: 0;
    }

    .flashes li {
      padding: 10px;
      margin-bottom: 10px;
      border-radius: 6px;
    }

    .flashes .success {
      background-color: #d4edda;
      color: #155724;
    }

    .flashes .error {
      background-color: #f8d7da;
      color: #721c24;
    }

    input[type="file"] {
      margin-bottom: 10px;
    }

    nav {
      background-color: white;
      padding: 2rem 2rem;
      color: black;
      align-items: center;
      justify-content: space-between;
      display: flex;
      cursor: pointer;
      height: 30px;
      box-shadow: 0 0 10px grey;
      position: sticky;
      top: 0;
    }

    .dropdown {
      width: 50%;
      padding: 10px;
      margin: 16px auto;
      display: block;
      border-radius: 8px;
    }

    .dropdown1 {
      width: 100%;
      padding: 8px;
      margin: 10px 0;
      border-radius: 8px;
    }

    .or-separator {
      color: #888;
      margin: 25px 0;
      font-size: 0.9em;
      text-transform: uppercase;
      text-align: center;
    }

    .nav-links {
      display: flex;
      gap: 2rem;
    }

    .footer {
      text-align: center;
      padding: 1rem;
      background: white;
      font-size: 0.9rem;
    }

    /* Loading Spinner */
    #loading {
      display: none;
      text-align: center;
      font-weight: bold;
      color: #333;
      margin: 20px 0;
    }

    .spinner {
      border: 5px solid #f3f3f3;
      border-top: 5px solid black;
      border-radius: 50%;
      width: 30px;
      height: 30px;
      animation: spin 1s linear infinite;
      display: inline-block;
      vertical-align: middle;
      margin-right: 10px;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
  </style>
</head>
<body>
  <nav>
    <div class="logo"><h1 style="font-size: 33px;">ExamPro AI</h1></div>
    <div></div>
    <div class="nav-links" id="navLinks">
      <a href="/history" style="color:black; margin-top: 22px;">History <i class="fa fa-history"></i></a>
      <a href="/logout" style="color:black; margin-top: 22px;">Logout <i class="fa fa-arrow-circle-left"></i></a>
    </div>
  </nav>

  <h2 style="float: left; margin-left: 36px; margin-top: 34px;">
    Welcome, <%= username %>
  </h2>

  <div class="container">
    <center>
      <h1>Ace your exams with AI-powered assistance</h1>
      <p>Select an exam, ask a question and select category to get started.</p>
    </center>

    <!-- ✅ Text-based question form -->
    <form method="POST" action="/dashboard">
      <select name="exam" class="dropdown" required>
        <option value="" disabled selected>Select an exam</option>
        <option value="Central Govt Exam">Central Govt Exam</option>
        <option value="NEET">NEET</option>
        <option value="JEE">JEE</option>
        <option value="State Board">State Board</option>
        <option value="UPSC">UPSC</option>
        <option value="SSC">SSC</option>
        <option value="Bank Exams">Bank Exams</option>
        <option value="Railway Exams">Railway Exams</option>
        <option value="Others">Others</option>
      </select>

      <textarea name="question" class="dropdown1" placeholder="Type question and select below to generate..." required></textarea>

      <h2 style="text-align: left;">Generate:</h2>

      <center>
        <button type="submit" name="action" value="answer">🔍 Explanation</button>
        <button type="submit" name="action" value="summarize">🧠 Summarize</button>
        <button type="submit" name="action" value="keywords">🧷 Keywords</button>
        <button type="submit" name="action" value="solution">📝 Solution</button>
        <br /><br />
        <button type="submit" name="action" value="concept">❓Concept</button>
        <button type="submit" name="action" value="hint">💡Show Hint</button>
        <button type="submit" name="action" value="translate">🌐 Translate</button>
        <button type="submit" name="action" value="questions">✅ Questions</button>
        <button type="submit" name="action" value="tips">🚀 Boost with Tips</button>
        <br /><br />
        <button type="submit" name="action" value="shortnotes">🎯 Short notes</button>
      </center>
    </form>

    <!-- Loading spinner -->
    <div id="loading">
      <div class="spinner"></div>
      Generating Answer...
    </div>

    <div class="or-separator">OR</div>

    <!-- ✅ Image upload form -->
    <form action="/upload-doc" method="POST" enctype="multipart/form-data">
      <label>Upload Image of Question:</label>
      <input type="file" name="image" accept="image/*" required />
      <button type="submit">📷 Extract & Ask GPT</button>
    </form>

    <hr />

    <% if (answer) { %>
    <h3>Generated Answer:</h3>
    <div class="answer-box" id="answerBox"><%= answer %></div>
    <button onclick="clearAnswer()">🧹 Clear Answer</button>
    <% } %>

    <% if (messages && messages.length > 0) { %>
    <ul class="flashes">
      <% messages.forEach(({ category, message }) => { %>
      <li class="<%= category %>"><%= message %></li>
      <% }) %>
    </ul>
    <% } %>
  </div>

  <hr />
  <footer class="footer">
    <p>© 2025 ExamPro AI | Built for Smart Learning</p>
  </footer>

  <!-- Scripts -->
  <script>
    // Show loading spinner
    document.querySelectorAll("form").forEach(form => {
      form.addEventListener("submit", () => {
        document.getElementById("loading").style.display = "block";
      });
    });

    // Clear answer content
    function clearAnswer() {
      const box = document.getElementById("answerBox");
      if (box) box.innerHTML = "";
    }

    // Auto-scroll to answer
    window.onload = function () {
      const box = document.getElementById("answerBox");
      if (box) box.scrollIntoView({ behavior: "smooth" });
    };
  </script>
</body>
</html>
